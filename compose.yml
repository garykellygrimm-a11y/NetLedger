services:
  db:
    image: postgres:18
    environment:
      POSTGRES_USER: netledger
      POSTGRES_PASSWORD: netledger
      POSTGRES_DB: netledger
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - netledger-pg:/var/lib/postgresql

volumes:
  netledger-pg:
